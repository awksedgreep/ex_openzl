name: Precompile NIF binaries

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    name: Build ${{ matrix.target }} (OTP ${{ matrix.otp }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-linux-gnu
            runner: ubuntu-latest
            otp: "27"
            elixir: "1.19"
          - target: x86_64-linux-gnu
            runner: ubuntu-latest
            otp: "28"
            elixir: "1.19"
          - target: x86_64-linux-gnu
            runner: ubuntu-latest
            otp: "29"
            elixir: "1.20"
          - target: aarch64-linux-gnu
            runner: ubuntu-24.04-arm
            otp: "27"
            elixir: "1.19"
          - target: aarch64-linux-gnu
            runner: ubuntu-24.04-arm
            otp: "28"
            elixir: "1.19"
          - target: aarch64-linux-gnu
            runner: ubuntu-24.04-arm
            otp: "29"
            elixir: "1.20"
          - target: aarch64-apple-darwin
            runner: macos-14
            otp: "27"
            elixir: "1.19"
          - target: aarch64-apple-darwin
            runner: macos-14
            otp: "28"
            elixir: "1.19"
          - target: aarch64-apple-darwin
            runner: macos-14
            otp: "29"
            elixir: "1.20"

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install Erlang & Elixir
        uses: erlef/setup-beam@v1
        env:
          ImageOS: ubuntu24
        with:
          otp-version: ${{ matrix.otp }}
          elixir-version: ${{ matrix.elixir }}

      - name: Install CMake (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y cmake

      - name: Install CMake (macOS)
        if: runner.os == 'macOS'
        run: brew install cmake

      - name: Fetch dependencies
        run: mix deps.get

      - name: Precompile
        run: mix elixir_make.precompile
        env:
          CC_PRECOMPILER_CURRENT_TARGET: ${{ matrix.target }}
          ELIXIR_MAKE_CACHE_DIR: ${{ github.workspace }}/cache

      - name: Upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: nif-${{ matrix.target }}-otp-${{ matrix.otp }}
          path: cache/*.tar.gz
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install Erlang & Elixir
        uses: erlef/setup-beam@v1
        with:
          otp-version: "29"
          elixir-version: "1.20"

      - name: Fetch dependencies
        run: mix deps.get

      - name: Download all artifacts
        uses: actions/download-artifact@v5
        with:
          path: artifacts
          pattern: nif-*
          merge-multiple: true

      - name: List artifacts
        run: ls -la artifacts/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.tar.gz

      - name: Generate checksums
        run: mix elixir_make.checksum --all --ignore-unavailable
        env:
          ELIXIR_MAKE_CACHE_DIR: ${{ github.workspace }}/artifacts

      - name: Commit checksum.exs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          cp checksum.exs /tmp/checksum.exs
          git fetch origin main
          git checkout main
          cp /tmp/checksum.exs checksum.exs
          git add checksum.exs
          git diff --cached --quiet || git commit -m "Update checksum.exs for ${GITHUB_REF_NAME}"
          git push

      - name: Publish to Hex
        run: mix hex.publish --yes
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
